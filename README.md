## implementation of ROT13 algorithm in python

- simple function that takes in 
    - the string to 'rotate' or 'encode', 
    - alphabet type i.e 
        - latin (only 23 alphabets, without 'u' and 'w')
        - regular english alphabets (26 alphabets)
